server:
  port: 88
spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.ip:127.0.0.1}:${nacos.port:8848}
      config:
        server-addr: ${nacos.ip:127.0.0.1}:${nacos.port:8848}
  application:
    name: gateway
