spring:
  profiles:
    active: dev
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.ip:127.0.0.1}:${nacos.port:8848}
      config:
        server-addr: ${nacos.ip:127.0.0.1}:${nacos.port:8848}
        file-extension: yaml
server:
  port: 88